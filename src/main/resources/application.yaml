spring:
  application:
    name: wontaegyu

  datasource:
    url: jdbc:mariadb://localhost:3306/aidb
    username: root
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: 오빠네_프로젝트_구글이메일
    password: 구글_앱_비밀번호
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true